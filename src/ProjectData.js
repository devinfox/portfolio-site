class ProjectData {
    constructor(img, title, descr, link) {
        this.img = img;
        this.title = title;
        this.descr = descr;
        this.link = link;
    }
}

export default ProjectData;